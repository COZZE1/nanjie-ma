# Photography files

Put series folders by year or project name. Same naming rule as painting: English slug, ISO date.

The public Photography page is `docs/photography.html`. It stays empty-framed until images are copied to `docs/assets/img/`.
